import Link from "next/link";

import type { AnalyticsDateRange } from "@/features/analytics/date-range";

export function DateRangeFilter({ basePath, range }: { basePath: string; range: AnalyticsDateRange }) {
  const options = [{ label: "Today", value: "today" }, { label: "7 days", value: "7d" }, { label: "30 days", value: "30d" }] as const;
  return <section className="mt-5 rounded-[1.5rem] border border-slate-200 bg-white p-3 shadow-sm"><div className="grid grid-cols-3 gap-2">{options.map((option) => <Link aria-current={range.preset === option.value ? "page" : undefined} className={`flex min-h-11 items-center justify-center rounded-xl px-2 text-sm font-semibold ${range.preset === option.value ? "bg-emerald-700 text-white" : "bg-slate-50 text-slate-600"}`} href={`${basePath}?range=${option.value}`} key={option.value}>{option.label}</Link>)}</div><details className="mt-2 rounded-xl bg-slate-50 px-3 py-2" open={range.preset === "custom"}><summary className="cursor-pointer py-1 text-sm font-semibold text-slate-600">Custom range</summary><form className="mt-3 grid gap-3 sm:grid-cols-[1fr_1fr_auto]" method="get"><input name="range" type="hidden" value="custom" /><label className="text-xs font-semibold text-slate-500">From<input className="mt-1 min-h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm" defaultValue={range.fromDate} max={range.toDate} name="from" required type="date" /></label><label className="text-xs font-semibold text-slate-500">To<input className="mt-1 min-h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm" defaultValue={range.toDate} max={range.toDate} name="to" required type="date" /></label><button className="min-h-11 self-end rounded-xl bg-slate-900 px-5 text-sm font-semibold text-white" type="submit">Apply</button></form></details></section>;
}
